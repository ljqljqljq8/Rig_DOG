# Branch Notes

This branch is a cleaned packaging branch for the current Rig Dog working setup.

- Firmware source remains at repo root.
- Local face service lives in `mcp_service/`.
- Human face photos and embeddings are intentionally excluded from git.

See `docs/face_mcp_status.md` for the current feature status and integration notes.
